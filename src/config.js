// global configuration
Config = {
	results_per_page: 25, //github default results per page is 30,
	maxSummaryLength: 140,
	base_url: 'https://api.github.com/repos/rails/rails/' //github api endpoint for rails repo
};
